import React from "react";

describe('SignInOrOutLink', function() {
    it('Testing SignInOrOutLink component', (done)=>{
        const TestUtils = require('react-dom/test-utils')
        const React = require('react')
        const ReactDOM = require('react-dom')
        const temp = require('../public/src/SignInOrOutLink.js')
        const SignInOrOutLink = temp.default

        const fD = ReactDOM.findDOMNode

        done()
    })
})